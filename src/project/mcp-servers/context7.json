{
  "name": "context7",
  "description": "Library documentation lookup — fetches current docs instead of relying on training data",
  "when": "Any project that uses external libraries",
  "config": {
    "type": "http",
    "url": "https://mcp.context7.com/mcp",
    "headers": {
      "CONTEXT7_API_KEY": "${CONTEXT7_API_KEY}"
    }
  }
}
